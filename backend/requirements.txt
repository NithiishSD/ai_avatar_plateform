# ==============================================================================
# AI Avatar Platform - Dependencies for Python 3.10
# Optimized for CUDA 12.1 (NVIDIA RTX GPUs) & Open-Source AI Avatar Stack
# ==============================================================================

# PyTorch (supports CUDA 12.1 acceleration when GPU present; falls back to CPU automatically)
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchaudio>=2.2.0
torchvision>=0.17.0

# Core Scientific & Array Processing (Must stay <2.0 for TTS / Numba stability)
numpy>=1.24.3,<2.0.0
scipy>=1.11.0,<1.14.0
numba>=0.58.0,<0.60.0

# FastAPI & Web API Framework
fastapi>=0.110.0,<0.116.0
uvicorn[standard]>=0.28.0,<0.35.0
pydantic>=2.6.0,<2.11.0
python-multipart>=0.0.9
httpx>=0.27.0

# Task Queue & Storage Infrastructure
celery>=5.3.6,<5.5.0
redis>=5.0.0,<6.0.0
python-dotenv>=1.0.0

# Milestone 1: Voice Engine & Neural Text-to-Speech (TTS)
# -------------------------------------------------------
# Model 1: Kokoro-82M (fast, English real-time)
kokoro>=0.8.4
soundfile>=0.12.1
librosa>=0.10.1
inflect>=7.0.0
phonemizer-fork>=3.3.0

# Model 2 (High Quality / Multilingual): Higgs TTS 2 (3B)
# Loaded via transformers pipeline — no separate pip install needed.
# HF model: bosonai/higgs-tts-2-3b-base  (VRAM: ~3 GB on RTX 4050)
transformers>=4.38.0,<4.48.0
accelerate>=0.28.0
huggingface_hub>=0.21.0
einops>=0.7.0

# Model 3 (Dialogue / Multi-speaker): Dia-1.6B (Nari Labs)
# Loaded via transformers AutoModel API (nari-labs/Dia-1.6B) to avoid the
# numpy>=2.2.4 conflict from the nari-tts package.
# NOTE: Do NOT install 'nari-tts' via pip — it pins numpy>=2.2.4 which
#       conflicts with the numpy<2.0.0 requirement for Numba/Coqui-TTS.
#       The transformers AutoModel path works without nari-tts.

# Coqui XTTS-v2 Engine Dependencies
# (TTS==0.22.0 is installed via 'pip install --no-deps TTS==0.22.0' to bypass legacy numpy==1.22.0 pin)
coqpit>=0.0.16
coqui-tts-trainer
pysbd>=0.3.4
anyascii>=0.3.0
pypinyin>=0.50.0
jieba>=0.42.0
hangul-romanize>=0.1.0
jamo>=0.4.1
nltk>=3.8.0
g2pkk>=0.1.1
bangla>=0.0.6
bnnumerizer>=0.0.2
bnunicodenormalizer>=0.1.7
encodec>=0.1.1
unidecode>=1.3.2
num2words>=0.5.12
pandas>=1.4.0,<2.0.0
gruut[de,es,fr]==2.2.3

# Milestone 2: Lip Sync & Computer Vision Pipeline
opencv-python-headless>=4.8.0,<5.0.0
mediapipe>=0.10.9
pillow>=10.0.0,<11.0.0
tqdm>=4.66.0
ffmpeg-python>=0.2.0

# Audio/Video Metric & Text Utilities
requests>=2.31.0
pyyaml>=6.0.1
